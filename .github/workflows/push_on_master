name: Push on master
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, reopened ]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Write key file
              run: echo "${{ secrets.APP_SIGN_KEY }}"
